// Paragraph block rendering is handled inline by TipTap's built-in paragraph extension.
// The ParagraphWithId functionality is managed through the BlockEditor component
// which adds blockId attributes to all paragraph nodes.
export { };
