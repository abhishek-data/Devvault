// Heading block rendering is handled inline by TipTap's built-in heading extension.
// The HeadingWithId functionality is managed through the BlockEditor component
// which adds blockId attributes to all heading nodes.
export { };
